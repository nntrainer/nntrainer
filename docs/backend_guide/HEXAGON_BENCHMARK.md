# Hexagon backend — benchmark and goals (Qwen3-0.6B, Snapdragon 8 Elite)

The single place where "where are we" and "where are we going" live. The
supervisor agent (`.claude/agents/hexagon-supervisor.md`) updates the
`nntrainer` rows from filled measurement handoffs
(`docs/measurements/`); HEXAGON.md §8 keeps the detailed tables. A snapshot
is published on the author's blog
(`dlwlzzero.github.io/_study/2026-09-16-WTD.md`).

Method for the `nntrainer` rows: `hexagon_e2e_test --chunk 128 --steps 64`,
decode median over steps 2–64, host wall time around each RPC
(HEXAGON.md §8.2). Two S25 Ultra units have been used and their cDSP
decode clocks differ (1.91 vs 2.05 GHz, HEXAGON.md §7 rule 9): rows name
the unit, tok/s is compared only within a unit, and DSP Mcycles per step
is the cross-unit number. At 4096 even the Mcyc figure moves with the
session on one unit (#53, 2026-09-18: +2.2 % between two orders 25 min
apart, +4.4 % between two sittings on the same day, +8.6 % against the
#23 unit with the *same* binary; HEXAGON.md §7 rule 9(e)), so a 4096
verdict is an A/B read inside one order of one sitting, never a 4096
cell against another day's row. GENIEX rows are from Qualcomm AI Hub
(https://aihub.qualcomm.com/models/qwen3_0_6b).

## Comparison table

| Runtime | Precision | Device | Backend | Context | Prefill tok/s | Decode tok/s | Notes |
|---|---|---|---|---|---|---|---|
| GENIEX_LLAMACPP | q4_0 | 8 Elite QPN | NPU | 512 | 3,409 | 70.3 | |
| GENIEX_LLAMACPP | q4_0 | 8 Elite QPN | NPU | 4096 | 2,550 | 27.5 | |
| GENIEX_LLAMACPP | q4_0 | 8 Elite QPN | CPU | 512 | 945 | 89.4 | |
| GENIEX_LLAMACPP | q4_0 | 8 Elite QPN | CPU | 4096 | 237 | 18.9 | |
| GENIEX_QAIRT | w4a16 | 8 Elite (Galaxy S25) | NPU | 512 | 8,207 | 121 | |
| GENIEX_QAIRT | w4a16 | 8 Elite (Galaxy S25) | NPU | 1024 | 7,503 | 112 | |
| GENIEX_QAIRT | w4a16 | 8 Elite (Galaxy S25) | NPU | 4096 | 3,546 | 57.6 | |
| GENIEX_QAIRT | w4a16 | 8 Elite QPN | NPU | 4096 | 7,574 | 115 | |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra) | NPU, HVX only | 512 | 192.1 | 27.7 | int8 per-channel weights, per-token int8 act; down_proj int16 act (M6 P4, 2026-09-16, SDK 6.0.0.2, v75 skel) |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra) | NPU, HVX only | 1024 | 118.2 | 22.9 | same |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra) | NPU, HVX only | 4096 | 27.3 | 9.4 | same |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra) | NPU, HVX only | 512 | 189.0 | 31.9 | same kernels, v75 skel rebuilt with SDK 6.4.0.1 / hexagon-clang 19.0.04 (#23, 2026-09-17, `docs/measurements/23-sdk64-baseline.md`); no regression vs P4; the 512 decode figure is ±5 % by construction (HEXAGON.md §8.2) |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra) | NPU, HVX only | 1024 | 117.1 | 23.2 | same |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra) | NPU, HVX only | 4096 | 27.1 | 9.6 | same |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY205ZMND`) | NPU, HVX only, v79-native skel | 512 | 207.4 | 31.4 | `HEX_ARCH=v79` build of the same sources (#23, 2026-09-17); PPL / top-1 track the v75 skel at every context. Pre-#35 quantiser decode (the four ±1 LSB v79 simulator failures, fixed by #35); kept as the v79 reference on this unit |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY205ZMND`) | NPU, HVX only, v79-native skel | 1024 | 131.2 | 25.3 | same |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY205ZMND`) | NPU, HVX only, v79-native skel | 4096 | 34.4 | 11.4 | same |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY10WM83Y`) | NPU, HVX only | 512 | 185.5 | 29.4 | same kernels as the SDK 6.4 rows, v75 skel built from the #33 tree (md5 `cc0f1725…`; its DSP diff against #23's `81dcaab4…` is comments, clang-format and one per-call token-id check), host logits buffer in rpcmem + `FASTRPC_MAP_STATIC` (#24, 2026-09-18, `docs/measurements/24-host-logits.md`). This is the P3/P4 unit, whose cDSP holds 1.91 GHz in the decode loop where the #23 unit held 2.05 GHz: DSP cycles match #23 within 1–6 % (65.1 vs 64.3 Mcyc @512), so the tok/s delta is the unit's clock, not the change (HEXAGON.md §7 rule 9). malloc / rpcmem / static decode steps are within 70 µs of each other; `--eval` 33.0884 / 189 = #23 |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY10WM83Y`) | NPU, HVX only | 1024 | 114.5 | 23.0 | same (85.0 Mcyc vs #23 84.2) |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY10WM83Y`) | NPU, HVX only | 4096 | 25.4 | 9.1 | same (225.9 Mcyc vs #23 213.0, +6 % after 168 s of sustained load, not repeated; the same +6 % recurs with the v79 skel in #35 and #23's own binary reads +8.6 % on this unit in #53 — a unit / session offset, not code) |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY10WM83Y`) | NPU, HVX only, **v79 shipping skel** | 512 | 198.7 | 30.9 | `HEX_ARCH=v79` default build since #35 (integer quantiser decode, simulator 13/13 on both arches; `docs/measurements/35-v79-skel.md` B1, 2026-09-18, SDK 6.4.0.1 skel md5 `8d8cdb26…`). 61.2 Mcyc/step vs #23's 60.1 on the other unit (+1.8 %); prefill −4.2 %. `--eval` on the P4 prompt **32.4497 / 184** (x86 33.0195 / 184: −1.73 %, top-1 exact) — the v79 accuracy record, see HEXAGON.md §8.2; `t512_23` 41.4947 vs x86 41.5466 (−0.12 %). A `-DHTP_FORCE_QF_HELPERS` build (B2) reproduces every PPL / top-1 digit for digit and is within 0.5 % in speed (§7 rule 1 closed) |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY10WM83Y`) | NPU, HVX only, **v79 shipping skel** | 1024 | 131.1 | 24.9 | same (77.8 Mcyc vs #23 76.5, +1.7 %; prefill −0.1 %); `--eval` 5.8595 / 692 (#23 v79 5.9509 / 692) |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY10WM83Y`) | NPU, HVX only, **v79 shipping skel** | 4096 | 32.7 | 11.2 | same, cooled re-run (first run 32.1 / 10.8); 186.8 Mcyc (first run 188.0) vs #23 177.6 = +5.2 / +5.9 %, prefill −4.9 / −6.6 %: outside the ±5 % gate, but the v75 #24 row above misses #23 by the same +6 % on this unit with a DSP diff of comments and one token-id check, so the 4096 cell was read as a unit / session effect — **confirmed by #53** (2026-09-18, `docs/measurements/53-4096-unit-split.md`): #23 B's exact `aba0126e…` binary reads 197.3 / 192.9 Mcyc on this unit, indistinguishable from `hvx_impl`'s skel in the same sitting (0.1 %), so the offset is the unit and the session, not the code (HEXAGON.md §7 rule 9(e)). Still 17 % fewer cycles and +29 % prefill than v75 on the same unit and day (#24: 225.9 Mcyc / 25.4 tok/s); `--eval` 1.5623 / 3759 (#23 v79 1.5687 / 3758) |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY10WM83Y`) | NPU, HVX only, **v79 skel + cross-op weight prefetch** | 512 | 191.1 | 33.7 | `hvx/25-decode-prefetch` variant B (`docs/measurements/25-decode-prefetch.md`, 2026-09-18, SDK 6.4.0.2 workstation build `98b07caf…`, `-DHTP_PROF_FARF` on, pass 2 of a reversed double pass): **54.6 Mcyc/step** against 61.7 for the same tree with `-DHTP_MM_NO_PREFETCH` (A; A is #35 B1 +0.9 %, the environment check) = **−11.6 %**; `mm8` 24.3 → 16.1 Mcyc (−34 %), 0.5 Mcyc *under* the stream-only skel's pure DMA wait, so the W8A8 MACs are now hidden behind the weight stream. Host tok/s is at this session's 1.84 GHz (`pcycles_per_us` wandered 1743–2046); prefill Mcyc is 1.3 % under A, so the −4 % against #35's 198.7 is the clock, not the change. `--eval` 41.4947 / 162 = #35 B1, `E2E gen` byte-identical to A. Chunk 64 (D, ledger ⑨) read +9.9 % Mcyc and lower prefill: max-fit stays. Prefetch is the shipping default from #25 on |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY10WM83Y`) | NPU, HVX only, **v79 skel + cross-op weight prefetch** | 1024 | 123.7 | 26.3 | same; 73.7 Mcyc (#35 B1 77.8, −5.2 %). B equals the stream-only skel C within 0.1 % here: the prefetch has nothing left to win above 512, ATTN is 34.2 Mcyc = 46 % of the step (issue #58); `--eval` 5.8595 / 692 = #35 |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY10WM83Y`) | NPU, HVX only, **v79 skel + cross-op weight prefetch** | 4096 | 30.8 | 11.1 | same; 184.6 Mcyc (#35 B1 cooled re-run 186.8, −1.2 %, same unit and day; a different sitting, so within the ±5 % session spread #53 measured, not a gain); ATTN is 146.6 Mcyc = **79 % of the step** (issue #58); `--eval` 1.5623 / 3759 = #35 |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra, unit `R3CY10WM83Y`) | NPU, **weight-stream-only skel** (`-DHTP_MM_STREAM_ONLY`: every weight byte DMA'd, nothing multiplied — a ceiling, not a runtime) | 512 | — | **54 (ceiling)** | #25 variant C, pass 2: the 595,984,384 B/step (440.4 MB of layer weights + the 155.6 MB lm_head) land in `mm8` 16.6 + `mm16` 7.2 + `lg` 8.3 = **32.2 Mcyc = 18.5 ms at 1.74 GHz = 32.3 GB/s**, i.e. 54 tok/s if everything else were free. Per path: tiled DMA (q/k/v/o/gate/up) 37.0 GB/s, lm_head 32.5, row-major `down` 21 (issue #59). The step-level C figure (53.0 Mcyc, 32.9 tok/s; 26.2 at 1024, 10.9 at 4096) still runs ATTN + eltwise, and the handoff's "≈ 74 tok/s" quoted `mm8`+`mm16` only, i.e. without the lm_head's 26 % of the bytes — 54 is the number the goal reads. `mm8`+`mm16` is flat with context (23.8 / 25.9 / 26.4 Mcyc at 512 / 1024 / 4096), so the ceiling is ≈ 54–57 tok/s at every depth (≈ 54.5 / 56.3 at 1024 / 4096 with `lg` *derived* from the step total at the #25 read-back; the FARF logs `device_farf_1338{17,28}.log` are on the workstation, reading them with `summ_farf_prof.py` is a one-minute user to-do) |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra) | NPU, HVX + HMX | 512 | TODO | TODO | needs HexKL (stage 2) |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra) | NPU, HVX + HMX | 1024 | TODO | TODO | |
| nntrainer | W8A8 | 8 Elite (Galaxy S25 Ultra) | NPU, HVX + HMX | 4096 | TODO | TODO | |
| nntrainer | w4a8 | 8 Elite (Galaxy S25 Ultra) | NPU, HVX only | 512 / 4096 | TODO | TODO | 4-bit weight stream, issue #51 — folded into #65 (user decision 2026-09-18): the per-channel `w4cx` format of #65 S1 (#69) and the HVX `embed` / `LOGITS` / `down` nibble kernels of S3 (#71); filled from handoff H2. The 70.3 / 27.5 GENIEX_LLAMACPP cells stay this row's reference |
| nntrainer | Q4_0 weights, fp16 act (issue #60) | 8 Elite (Galaxy S25 Ultra) | CPU, `main` | 512 / 1024 / 4096 | pending | pending | **the floor every nntrainer NPU row is read against** (user decisions 2026-09-18: baseline = CPU from `main`; precision = `Q4_0-FP16`); handoff `docs/measurements/60-cpu-baseline.md` |

## Goals

| Stage | Metric | Now | Goal | Gap | Levers (follow-up ledger numbers) |
|---|---|---|---|---|---|
| 1 HVX only, W8A8 | decode @512 | 33.7 (#25 B, cross-op prefetch, 2026-09-18, unit `R3CY10WM83Y`, **54.6 Mcyc/step**; the #35 shipping skel read 30.9 / 61.2 Mcyc on the same unit, the #23 unit `R3CY205ZMND` 31.4 / 60.1 pre-#35) | **Relative to the CPU baseline (issue #60, user decision 2026-09-18)**: nntrainer on the S25 Ultra CPU from `main`, Qwen3-0.6B, measured by the user; the multiple is set once that row is filled. The provisional ≥ 60 was withdrawn as above the measured W8 weight-stream ceiling of **54 tok/s** (#25 C: 596 MB/step at 32.3 GB/s = 18.5 ms); the ceiling stays the physical bound the multiple must respect | pending #60 (1.6× to the ceiling) | The stream is done: B's `mm8`+`mm16`+`lg` = 33.6 of 54.6 Mcyc is within 4 % of C's pure DMA wait. Left at 512: **ATTN 17.3 Mcyc (32 %, ≈ 6.2 GB/s over 59 MB of fp16 KV; issue #58)**, `down` at 21 GB/s from row-major DDR instead of the 37 GB/s ring (ledger ⑬, issue #59, ≈ 3 Mcyc), eltwise `rest` 3.6 Mcyc, and #41 (clock in host idle gaps, app path only). ⑨ chunk 64 lost 9.9 %; ⑩'s `MM_TB` / `MM16_TB` are inert at m=1 (a prefill sweep, folded into #57); ⑫ closed by #24. 70.3 with int8 weights would need 41.9 GB/s over the *whole* step (issue #49), above the 37 the DMA path reaches on its own |
| 1 HVX only, w4a8 | decode @512 / @4096 | — | ≥ 70.3 / ≥ 27.5 (the GENIEX_LLAMACPP q4_0 NPU rows) | | 4-bit weight stream (≈ 307 MB/step) at the measured 32–37 GB/s = 8.3–9.6 ms/step; 70.3 (14.2 ms) then leaves ≤ 5.9 ms for ATTN + eltwise + LOGITS epilogue against 11.4 ms today (ATTN 9.4), so the tok/s targets need #58 (decode attention ≥ 3× down) as well as #51; #51's own gate is the stream-phase Mcyc (`mm8`+`mm16`+`lg` ≤ ≈ 0.55 × the W8 control's) plus the W4 PPL band. Nibble widening adds MACs to a phase that is exactly DMA-bound today, so B's compute may surface at half the bytes — the handoff needs the C-style stream-only variant next to it |
| 1 HVX only | decode @4096 | 11.1 (#25 B, `R3CY10WM83Y`, 184.6 Mcyc; #35 11.2 / 186.8, #23 unit 11.4 / 177.6; v75 9.1–9.6) | ≥ 27.5 | 2.5× | **ATTN is 146.6 of 184.6 Mcyc (79 %)** — 470 MB of fp16 KV read at ≈ 6.6 GB/s by a kernel that puts two kv heads on three of the six workers and re-streams K^T for each GQA query head; 27.5 tok/s = 36 ms/step needs ATTN ≤ 36 Mcyc at today's 38 Mcyc of non-ATTN work: issue #58 first, then #51 for the weight share. The prefetch changes nothing here (B = C). The 4096 cell's cross-session spread is settled by #53 (unit / session, not code; ±5 % between sittings on one unit), so #58's 4096 gate is read as an A/B inside one order, and a goal check at 4096 (2.5× away) is unaffected by it |
| 1 HVX only | prefill @512 | 191.1 (#25 B, `R3CY10WM83Y`, at a session clock 3–6 % below #35's: prefill Mcyc is 1.3 % under the no-prefetch control; #35 198.7, #23 unit 207.4; v75 185.5–189.0) | 1000 (interim) | 5.2× | tiled kernel efficiency — the `MM_TB` / `MM16_TB` / `MM16_R` device sweep is a prefill (m=128) question only and lives in #57 now; P5 attention (#26); 3,409 is out of HVX-only reach |
| 2 HVX + HMX | prefill @512 | — | ≥ 1000, then chase 3,409 | | HMX u8i8 matmul (specs/hexagon-hmx), needs HexKL |
| 2 HVX + HMX | decode @512 | — | follows the stage-1 W8A8 decision (the W8 stream ceiling of 54 tok/s is arch-independent: HMX does not change the bytes per step) | | same weight-stream bound as stage 1; HMX helps prefill, not the 596 MB stream |
| **3 W4 via upstream PR nntrainer#4327 (HMX u8×i4)** | prefill / decode @512 / 1024 / 4096 | — (first device numbers come from handoff H1, #70 S2) | **read against GENIEX_QAIRT w4a16 (Galaxy S25): 8,207 / 121, 7,503 / 112, 3,546 / 57.6**, with the #60 CPU row as the floor | | issue #65 (tracker; stages #68 S0 **merged 2026-09-21** (PR #72, `026af1e8`; the v79 simulator executes HMX, HEXAGON.md §7 rules 10–11) → #69 S1 (next) → #70 S2 / H1 → #71 S3 / H2, plan `docs/plans/65-w4-htp-port.md` §4; S1's x86 gate: per-channel int4 PPL ≤ 1.10 × W8, else a mixed i8 set, else needs-user): port the PR's `htp_backend` W4 path (HMX u8i4 matmul, DMA ring, u8 attention, kv quant) into our structure and run Qwen3-0.6B e2e. User decision 2026-09-18: the A8W8 HVX-only line (stages 1–2) ends with the issues already filed; HMX for that line comes later |

Decode and prefill do not move together: decode is weight-bandwidth plus
host-path bound, prefill is compute bound (HEXAGON.md §8.2). Reaching the
decode goal says nothing about prefill.

## Log

| Date | Change | Rows touched |
|---|---|---|
| 2026-09-16 | Table moved here from the blog draft; goals split into stage 1 / stage 2 (plan 0000) | all |
| 2026-09-21 | Supervisor: #68 (S0) closed, PR #72 merged at `026af1e8` — HexKL linked, 8 MB VTCM + HMX arena above the HVX kernels' unchanged 4 MB, `hmx` is the 14th simulator test and the v79 simulator executes HMX bit-exact (HEXAGON.md §7 rules 10–11 added, §9 direction bullet updated). No new row: S0 has no device number. #69 (S1, x86-only gate, no device step) is the next implementer target; #58 stays `needs-measurement` (user measuring) | stage 3 goal row |
| 2026-09-18 | #24 host logits measurement (`docs/measurements/24-host-logits.md`): three rows on the P4 unit `R3CY10WM83Y` with the rpcmem + `FASTRPC_MAP_STATIC` logits return; ledger ⑫ closed as "not a lever"; unit clock caveat added to the method and the goal "Now" | nntrainer HVX-only rows, stage 1 W8A8 goal, method |
| 2026-09-18 | #35 v79 shipping-skel measurement (`docs/measurements/35-v79-skel.md`, unit `R3CY10WM83Y`): three v79 rows added, goal "Now" moved to them; #23's v79 rows re-labelled with their unit; the 4096 cell's +6 % cross-session spread filed as issue #53; HEXAGON.md §7 rule 1 closed (IEEE vs qf helpers identical on v79 silicon), rule 9 corrected and extended | nntrainer v79 rows, stage 1 goals, method |
| 2026-09-18 | Supervisor (evening cycle): #65 split into four `state:planned` `prio:p0` stage sub-issues #68 (S0) #69 (S1) #70 (S2 + H1) #71 (S3 + H2, closes #51); #65 stays the tracker. Order #68 → #60 (while #68 is in review) → #69; the p2 queue (#59 #57) waits until S1's PPL band is known. HEXAGON.md §9 gained the direction bullet; plan 0000 §1 gained the stage-3 row and the #60 CPU-floor sentence | stage 3 goal row, w4a8 HVX-only row's note |
| 2026-09-18 | User decisions (evening): (a) the CPU baseline precision is `Q4_0-FP16` (#60 unblocked); (b) the A8W8 HVX-only line ends with the issues already filed, HMX for it later; (c) new top goal, issue #65: bring upstream PR nntrainer#4327's W4 (HMX u8×i4) path into our structure and measure Qwen3-0.6B e2e, read against GENIEX_QAIRT. Stage 3 row added to the goals; #51's HVX w4g128 plan is on hold until the planner of #65 says whether it is superseded | goals, CPU row |
| 2026-09-18 | User decision: the goals are read relative to nntrainer's own CPU baseline (Qwen3-0.6B, S25 Ultra, `main`), issue #60; the proposed fixed ≥ 40 replacement is dropped, the goal cell and the CPU row point at #60 | stage 1 W8A8 goal, CPU row |
| 2026-09-18 | #25 cross-op prefetch measurement (`docs/measurements/25-decode-prefetch.md`, unit `R3CY10WM83Y`, v79, SDK 6.4.0.2): three prefetch rows (54.6 / 73.7 / 184.6 Mcyc; −11.6 % at 512, prefetch on by default, chunk 64 rejected) and the weight-stream-only ceiling row (**54 tok/s @512**, 32.3 GB/s — the handoff's 74 excluded the lm_head bytes); the provisional ≥ 60 W8A8 goal is withdrawn as above the ceiling, replacement ≥ 40 proposed as needs-user; ATTN identified as 32–79 % of the decode step (issue #58), `down` DDR path as 21 vs 37 GB/s (issue #59); the w4a8 row's arithmetic re-based on the measured rates; HEXAGON.md §7 rule 9(d) (warm-up +8.5 %), §8.2 "#25" block, §9 | nntrainer prefetch + ceiling rows, all stage-1 goals, stage-2 decode goal |
| 2026-09-18 | #53 4096 unit-split measurement (`docs/measurements/53-4096-unit-split.md`, unit `R3CY10WM83Y`, v79, SDK 6.4.0.1): #23 B's exact binary (`aba0126e…`) and `hvx_impl`'s skel are indistinguishable inside one sitting (0.10 / 0.004 %, same 64 tokens, `--eval` digit-identical to their references) while both read +8.6 % over #23 B's 177.6 Mcyc on this unit — the +6 % 4096 offset is unit / session state, not code; between-order drift 2.2 %, between-sitting 4.4 %. No new row (no new shipping number); the three 4096 notes, the method paragraph and the @4096 goal cell now cite it; HEXAGON.md §7 rule 9(e) rewritten as a standing rule (4096 A/B inside one order, battery °C and charge state recorded), §8.2 "#53" block, §9. Issue #53 closed | 4096 rows' notes, method, decode @4096 goal |
| 2026-09-18 | Stage-1 W8A8 decode goal set to the weight-stream bound (≥ 60 provisional) after issue #49's arithmetic; 70.3 / 27.5 become the stage-1 w4a8 row (issue #51) | stage 1 goals, w4a8 row |
| 2026-09-17 | SDK 6.4 baseline from `docs/measurements/23-sdk64-baseline.md` (#23): v75 rows re-measured, no regression; v79-native skel rows added as ledger ④ data; goal "Now" moved to the v75 re-measurement | nntrainer HVX-only rows, stage 1 goals |
| 2026-09-17 | #35: quantiser decode made jam-independent, simulator 13/13 on both arches; user decision: v79 is the primary arch (S25 Ultra), v75 wrapped up. Goal "Now" moved to the v79 rows pending the device re-measurement of the fixed decode (`docs/measurements/35-v79-skel.md`, B1/B2, A optional; measured 2026-09-18, entry above) | v79 rows' note, stage 1 goals |
