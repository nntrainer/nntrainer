# 2026-09-17 인수인계 — Hexagon 큐 상태와 사용자 to-do

세션: https://claude.ai/code/session_01AETWMAyrqeDgFwAHv5es83 (원격 컨테이너, SDK/기기 없음).
계약: `docs/plans/0000-agent-system-and-env.md`. 트래킹 이슈: #42.

## 1. 브랜치 한 장 요약

모든 산출물은 **`claude/issue-23-device-measurement-crvngx`** 한 브랜치에 있다 (`main` @ `a7ea056` 위 39커밋).
아래 순서로 쌓여 있고, 병합할 때 이 경계로 자른다.

| 구간 | 커밋 | 내용 | 병합 대상 |
|---|---|---|---|
| ① | `main..f66bf3b` (18) | #34 클린 히스토리: 스쿼시 15 + 임포트 파일 clang-format + `/**` doxygen + #28 CI 트리거 | **`hvx_impl`을 통째로 교체** (강제 푸시) |
| ② | 8 + 2 | #23 브랜치 8커밋 rebase + read-back 문서(`74992ae` 상당) + §5.2/§9/원장 정정 | `hvx_impl` PR (#23) |
| ③ | 1 | `[docs] Plan the host logits transport measurement (#24)` | #24 PR에 포함 |
| ④ | 4 + 1 | #33 구현 4커밋 + `static_assert` 수정 | `hvx_impl` PR (#33), ②에 의존(§5.2 문단) |
| ⑤ | 4 + 1 | #24 구현 4커밋 + doxygen 오프너 수정 | 측정 후 `hvx_impl` PR (#24), ④에 의존 |

`hvx/history-clean`(구버전), `hvx/23-sdk64-baseline`, `hvx/33-dsp-bounds`는 이 브랜치가 대체한다. 이 세션은 위 브랜치 외에는 푸시 권한이 없다(403).

## 2. 이슈 상태

| 이슈 | 라벨 | 상태 |
|---|---|---|
| #23 SDK 6.4 baseline | measured | 측정·read-back 완료. PR만 남음 (구간 ②) |
| #33 DSP bounds | review | 게이트 2·3·4 PASS(워크스테이션). STAT 비트 동일 확인은 다음 sim 로그 때 |
| #24 host logits | needs-measurement | 구현 완료, **기기 핸드오프 대기** (`docs/measurements/24-host-logits.md`) |
| #32 W8_CX producer | planned p2 | (b) 결정. 문서 2곳 + 위저드 md5 경고 한 줄. 다음 구현 후보 |
| #35 v79 skel | needs-plan p1 | 재작성됨(IEEE `#error` 폐기 → sim 4건 통과/바운드 → 기기 → 기본값 전환). 다음 계획 후보 |
| #25, #26, #36–#38 | needs-plan | 변동 없음. 순서: #35 → #26 → #25; #36은 #33 머지 후 |
| #40 causal_lm hex 출력 | needs-plan p2 | 서브트리 밖, 한 줄 수정 |
| #41 DSP 유휴 구간 다운클럭 | needs-plan p1 | #24 핸드오프의 `--eval` `pcycles_per_us`가 첫 숫자 |
| #28, #34 | needs-user | 구간 ①의 강제 푸시로 동시에 해결 |
| #39 | closed | 실수로 열린 PR, 닫음 |

## 3. 사용자 to-do (순서대로)

### A. 기기 측정 — #24 핸드오프 (≈25분, 우선)
`docs/measurements/24-host-logits.md` 그대로. 요약:
```bash
git fetch && git checkout claude/issue-23-device-measurement-crvngx
source /local/mnt/workspace/Qualcomm/Hexagon_SDK/6.4.0.1/setup_sdk_env.source; export ANDROID_NDK=/opt/android-ndk-r26d
grep -n "FASTRPC_MAP_STATIC\|FASTRPC_MAP_FD_DELAYED\|remote_register_buf" $HEXAGON_SDK_ROOT/incs/remote.h     # step 0, 출력은 Notes에
./tools/hexagon/build_host_test.sh                       # 하네스만; skel은 #33 게이트 4 산출물 재사용
md5sum build_hexagon/skel/libnntr_htp_skel.so build_hexagon/host/hexagon_rpc_test build_hexagon/host/hexagon_e2e_test
./tools/hexagon/run_device_test.sh && python3 tools/hexagon/check_rpc_log.py logs/hexagon/device_test_<stamp>.log
for m in malloc rpcmem static; do ./tools/hexagon/run_e2e_test.sh /tmp/qwen3_full -- --tokens /tmp/t512.i32 --chunk 128 --steps 64 --logits-mem $m; done
for m in malloc rpcmem;        do ./tools/hexagon/run_e2e_test.sh /tmp/qwen3_full -- --tokens /tmp/t512.i32 --eval --logits-mem $m; done
./tools/hexagon/run_e2e_test.sh /tmp/qwen3_full   -- --tokens /tmp/t1024.i32 --chunk 128 --steps 64 --logits-mem <winner>
./tools/hexagon/run_e2e_test.sh /tmp/qwen3_full4k -- --tokens /tmp/t4096.i32 --chunk 128 --steps 64 --logits-mem <winner>
```
표를 채워 같은 브랜치에 커밋·푸시하고 #24를 `state:measured`로. `logs/hexagon/`도 함께 푸시하면 #33의 STAT 확인까지 한 번에 끝난다.
`--logits-mem static`이 `register_static failed`로 끝나면 그것도 결과다(기록하고 계속).

### B. `hvx_impl` 교체 (#34, #28)
```bash
git push --force-with-lease=hvx_impl:c33b2c9 origin f66bf3b1:hvx_impl   # 구간 ① 18커밋
git push origin --delete hvx/fix-container-sdk-env hvx/w8cx-from-hf hvx/history-clean   # 머지·대체된 브랜치
```
교체 후 #34, #28을 닫는다(`completed`). 확인: PR #39가 실패했던 `Static checks`·`cpp-linter`가 다음 hvx PR에서 통과해야 한다.

### C. PR 분리 (감독 에이전트에게 맡겨도 됨)
```bash
git checkout -b hvx/23-sdk64-baseline-v2 <구간 ② 마지막 커밋>   # = "[docs] Correct where the v79 simulator failures live (#35)"
git push -u origin hvx/23-sdk64-baseline-v2 && PR → hvx_impl (#23)
```
#23 머지 후 #33(구간 ③④), 측정 후 #24(구간 ⑤) 같은 방식. PR 본문은 `.github/PULL_REQUEST_TEMPLATE.md` (커밋당 `<details>`).

### D. 미뤄 둔 sim (PR 머지 전)
```bash
HEX_ARCH=v75 ./tools/hexagon/build_sim_test.sh
HEX_ARCH=v75 ./tools/hexagon/run_sim_test.sh profile acc     # STAT max_abs=0.077216 max_rel=98.2637
for t in smoke pool exp quant matmul matmul_dma rmsnorm rope eltwise embed attn logits graph; do HEX_ARCH=v75 ./tools/hexagon/run_sim_test.sh $t || break; done
```
#24는 DSP 바이트를 안 바꾸므로 STAT가 #33 실행값과 비트 동일해야 한다. `logs/hexagon/sim_*` 푸시.

### E. 다음 사이클 (`/hexagon-cycle`)
1. 감독: #24 measured 처리, #41 첫 숫자 기록, 병합 계획(§3 C) 확정.
2. 계획: #35 (재작성본).
3. 구현: #32 (작음, sim 불필요) 또는 #35 계획 완료 시 #35.

## 4. 이 세션에서 검증된 것 / 안 된 것

* 검증됨: 구간 ①의 clang-format/doxygen 트리가 **실제로 컴파일되고 sim 13/13 통과** (#33 게이트 2–4를 이 브랜치에서 실행). x86 게이트(`LOWER_TEST`, oplist header) 매 통합마다 PASS. CI 스크립트(doxygen-tag, newline, signed-off-by, clang-format-14 dry-run, `git diff --check`) 전 파일 0건.
* 안 됨: #24 하네스 컴파일(`rpcmem.h`/`remote.h` 스텁으로 문법만), `hexagon_ref_run --eval` PPL 불변 확인(모델 없음), STAT 비트 동일 판독(로그 미전달).
* 계약 편차: #23 기기 바이너리는 컨테이너가 아닌 워크스테이션 SDK 6.4.0.1 빌드(원장 ⑭에 기록). 이 세션의 GitHub MCP는 이슈/PR 쓰기가 되므로 계약 §4의 "gh만" 문구는 갱신 대상.
